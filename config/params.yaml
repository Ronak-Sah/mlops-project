data_ingestion:
  train_set_size: 0.7
  test_set_size: 0.2
  val_set_size: 0.1
  
data_transformation:
  scaler: "StandardScaler"

model_trainer:
  booster: gbtree
    # gbtree, gblinear or dart
  device: cuda
  learning_rate: 0.3
  max_depth: 6
  early_stopping_rounds: 10
  n_estimators: 1000